Architecture docs — next steps

Files added:
- `dependency_diagram.md` — shows project dependency directions (mermaid).
- `erd.md` — high-level ER diagram (mermaid).

Next actions I will perform (in order):
1. Produce a complete list of entities and every property (PKs, FKs, uniques, indexes). 
2. Produce delete behaviors, concurrency requirements, and transaction boundaries.
3. Produce domain rules and application use cases mapped to features.
4. Generate a detailed ERD per bounded context and produce an implementation plan.

I will now start Step 2: list every entity and property, primary/foreign keys, and unique constraints.
